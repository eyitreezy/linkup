import { MatchMakerPool } from '@/components/matchmaker/MatchMakerPool';

/** Deep-link route — full interactive pool (gate handled at /matchmaker root). */
export default function MatchMakerPoolScreen() {
  return <MatchMakerPool />;
}
