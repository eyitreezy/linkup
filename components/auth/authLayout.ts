import { spacing } from '@/constants/theme';

/** Horizontal inset for auth glass card — hero copy uses the same edges. */
export const AUTH_CARD_MARGIN_H = spacing.md;
export const AUTH_CARD_PADDING_H = spacing.lg;
