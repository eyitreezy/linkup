import { AppShellBackground } from '@/components/ui/AppShellBackground';

/** Inbox / agreement / escrow screens — branded shell backdrop. */
export function DiscoveryGradientBg() {
  return <AppShellBackground />;
}
