node_modules/
.expo/
dist/
web-build/
*.log
.env
.env.local
.DS_Store

# Private keys (Firebase Admin / FCM service account, etc.) — keep local or in a vault
secrets/

# Firebase Android config (contains API keys) — download from Firebase Console per machine / CI
google-services.json

# Android SDK path (machine-specific — copy from local.properties.example)
android/local.properties

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli